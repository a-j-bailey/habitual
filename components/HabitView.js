import { Text, View } from 'react-native';


const HabitView = ({sheetRef, title}) => (
    <View
      style={{
        backgroundColor: '#fff',
        padding: 16,
        height: 450,
      }}
    >
        <Text>Title:</Text>
    </View>
    
);

export default HabitView;